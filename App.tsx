import {SafeAreaView, ScrollView, Text} from 'react-native';
import React from 'react';

const App = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>Application</Text>
      </ScrollView>
    </SafeAreaView>
  );
};

export default App;
